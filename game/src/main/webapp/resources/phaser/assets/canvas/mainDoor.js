
// -- user code here --

/* --- start generated code --- */

// Generated by  1.5.1 (Phaser v2.6.2)


/**
 * mainDoor.
 */
function mainDoor() {
	
	Phaser.State.call(this);
	
}

/** @type Phaser.State */
var mainDoor_proto = Object.create(Phaser.State.prototype);
mainDoor.prototype = mainDoor_proto;
mainDoor.prototype.constructor = mainDoor;

mainDoor.prototype.init = function () {
	
	this.scale.scaleMode = Phaser.ScaleManager.SHOW_ALL;
	this.scale.pageAlignHorizontally = true;
	this.scale.pageAlignVertically = true;
	this.stage.backgroundColor = '#000000';
	
};

var logo_bgm; // 배경음악

mainDoor.prototype.create = function () {
	var mainLogo = this.add.sprite(31.0, 1.0, '00_TITLElogo');
	mainLogo.scale.setTo(0.23545088272407333, 0.22921686778606387);
	mainLogo.inputEnabled = true;
	mainLogo.events.onInputDown.add(logo_callJapanMap,this);
	
	/* 배경음악 설정 */
	logo_bgm = this.add.audio('title_vol1');
	logo_bgm.play(); // 배경음악 재생
	logo_bgm.loopFull(); // 배경음악 무한반복
};

/* --- end generated code --- */
// -- user code here --

/* 페이지 전환 함수 */
function logo_callJapanMap(){
	logo_bgm.pause(); // 배경음악 일시 정지
	logo_bgm.destroy(); // 배경음악 삭제
	
	if (clearFlag == false) {
		this.game.state.start("prologue"); // 프롤로그로 시작
	} else {
		this.game.state.start("japanMap"); // 지도화면으로 시작
	}
}

